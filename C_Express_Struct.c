#include <stdio.h>

struct Coordinates {
	double x;
	double y;
};

int main() {


	/* 572p Ex
	struct Coordinates p1;
	struct Coordinates p2;


	double w, h;
	printf("왼쪽 상단의 좌표 입력 : ");
	scanf("%lf %lf", &p1.x, &p1.y);

	printf("오른쪽 하단의 좌표 입력 : ");
	scanf("%lf %lf", &p2.x, &p2.y);

	w = p2.x - p1.x;
	h = p1.y - p2.y;
	printf("넓이 : %lf, 둘레 : %lf", w * h, 2 * (w + h));
	*/



	return 0;
}